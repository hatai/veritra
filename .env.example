# local sqld over HTTP
LIBSQL_URL=http://127.0.0.1:8080
LIBSQL_AUTH_TOKEN=

# better-auth — required at runtime (use .dev.vars for local wrangler dev)
AUTH_SECRET=change-me-to-a-random-32-char-secret
BASE_URL=http://localhost:8787
